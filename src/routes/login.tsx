import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { ArrowLeft, Eye, EyeOff } from "lucide-react";
import { FormEvent, useState } from "react";
import { Brand } from "../components/brand";
import { Button } from "../components/button";

export const Route = createFileRoute("/login")({ head: () => ({ meta: [{ title: "Sign in — Reach Ledger" },{ name: "description", content: "Sign in to your Reach Ledger workspace." },{ property: "og:title", content: "Sign in — Reach Ledger" },{ property: "og:description", content: "Access your LinkedIn Ads outreach workspace." },{ property: "og:type", content: "website" },{ name: "twitter:card", content: "summary" }] }), component: LoginPage });

function LoginPage() { const navigate=useNavigate(); const [show,setShow]=useState(false); const [error,setError]=useState(""); const submit=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault(); const fd=new FormData(e.currentTarget); if(!fd.get("email")||!fd.get("password")){setError("Enter both your email and password."); return;} navigate({to:"/accounts"});}; return <main className="grid min-h-screen bg-card lg:grid-cols-[1fr_0.9fr]"><section className="flex min-h-screen flex-col px-6 py-6 md:px-12 lg:px-16"><Brand /><div className="mx-auto my-auto w-full max-w-md py-12"><Link to="/" className="mb-8 inline-flex items-center gap-2 text-sm font-semibold text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4" />Back to home</Link><p className="text-xs font-bold uppercase text-primary">Welcome back</p><h1 className="mt-2 text-4xl font-extrabold tracking-tight">Sign in to Reach Ledger</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">Continue to your LinkedIn Ads engagement workspace.</p><form className="mt-9 space-y-5" onSubmit={submit}><label className="block text-sm font-semibold">Work email<input name="email" type="email" placeholder="you@company.com" className="mt-2 h-12 w-full rounded-md border border-input bg-background px-4 outline-none transition focus:border-primary focus:ring-2 focus:ring-primary/15" /></label><label className="block text-sm font-semibold"><span className="flex justify-between"><span>Password</span><button type="button" className="text-xs font-semibold text-primary hover:underline">Forgot password?</button></span><span className="relative mt-2 block"><input name="password" type={show?"text":"password"} placeholder="Enter your password" className="h-12 w-full rounded-md border border-input bg-background px-4 pr-12 outline-none transition focus:border-primary focus:ring-2 focus:ring-primary/15" /><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-3 text-muted-foreground" aria-label={show?"Hide password":"Show password"}>{show?<EyeOff className="size-5"/>:<Eye className="size-5"/>}</button></span></label>{error&&<p role="alert" className="rounded-md bg-danger-soft px-3 py-2 text-sm font-medium text-danger">{error}</p>}<Button className="w-full" size="lg">Sign in</Button><p className="text-center text-xs text-muted-foreground">Design preview — use any email and password.</p></form></div></section><aside className="hidden bg-brand-soft p-12 lg:flex lg:flex-col lg:justify-between"><div className="ml-auto rounded-full bg-card px-3 py-1 text-xs font-bold text-primary">LinkedIn Ads → CRM</div><blockquote className="max-w-xl"><p className="text-4xl font-extrabold leading-tight">“We knew the organisation was interested before the first outreach.”</p><p className="mt-5 text-sm leading-6 text-muted-foreground">Find the companies where someone watched, clicked, or engaged—then reach the right person with a relevant reason to talk.</p></blockquote><div className="grid grid-cols-3 gap-3">{[["2,264","companies"],["395","completions"],["194","warm signals"]].map(([v,l])=><div className="border-t border-primary/25 pt-4" key={l}><p className="text-2xl font-extrabold">{v}</p><p className="text-xs text-muted-foreground">{l}</p></div>)}</div></aside></main> }
